namespace AdBlocker.FMOD.Radio.RadioStations.Speakers {
    public enum SpeakerRole : byte {
        Host, // News, Stories, Comments, ...
        Ads,
        InterviewGeneric,
        InterviewMayor
    }
}